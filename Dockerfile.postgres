# PostgreSQL Dockerfile
FROM postgres:16-alpine

# Init scripts are mounted from the host via docker-compose:
# ./backend/init-scripts:/docker-entrypoint-initdb.d:ro

# Expose default PostgreSQL port
EXPOSE 5432
